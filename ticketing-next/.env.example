# Copy to .env and set your connection string.
# Local PostgreSQL (as set up on this machine):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ticketing?schema=public"
# Production (Vercel): set DATABASE_URL in the Vercel dashboard to your hosted
# Postgres (Neon / Supabase / Vercel Postgres) — do NOT commit that value.
